const LOCAL_DEFAULTS = {
    'WEIGHT': "60",
    'BOTTLES': 0,
    'TIME': 0,
    'GENDER': [
        // Default gender is first variable under gender
        'Female', 
        'Male'
    ]
}

Object.freeze(LOCAL_DEFAULTS)

export default LOCAL_DEFAULTS